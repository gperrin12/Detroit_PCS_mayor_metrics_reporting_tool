# Copy to secrets.toml locally (gitignored) or paste into Streamlit Cloud → App settings → Secrets

SMARTSHEET_TOKEN = "paste_your_token_here"

# Shared dashboard password (Stephani + team). Required to open the app when set.
APP_PASSWORD = "choose_a_strong_shared_password"
